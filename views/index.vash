@{
    model.title = 'Home';
}

@html.extend('layout', function(model) {
    @html.block('body', function(model) {
        <h2>@model.title</h2>
        <p>Welcome to Pi Peripherals!</p>
        <a href="/display" class="btn btn-lg btn-primary">Display</a>
    })
})
